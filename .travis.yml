language: node_js
cache:
  - bundler
node_js:
  - "6"
before_install:
  - if [[ `npm -v` != 3* ]]; then npm i -g npm@3; fi
  - rvm use 2.5.1 --install --fuzzy
  - ./gitclone.sh
install:
  - bundle install
  - npm install
script:
  - grunt build
after_success:
  - ./gitpush.sh